# Программа загадывает число от 0 до 1000. Необходимо угадать число за 10 попыток. Программа должна подсказывать “больше” или “меньше” после каждой попытки. Для генерации случайного числа используйте код:
# from random import randintnum = randint(LOWER_LIMIT, UPPER_LIMIT)
from random import randint
n = randint(0, 1000)
i = 1
print("Загадано число от 0 до 1000. У Вас есть 10 попыток, чтобы его отгадать. По ходу ваших ответов, Вы будете получать подсказки")
while i <= 10:
    u = int(input(str(i) + '-я попытка: '))
    if u > n:
        print('Меньше')
        print("Вы близки к результату" if abs(n - u) <= 2 else "Вы далеки от результата")
    elif u < n:
        print('Больше')
        print("Вы близки к результату" if abs(n - u) <= 2 else "Вы далеки от результата")
    else:
        print('Вы угадали с %d-й попытки' % i)
        break
    i += 1
else:
    print("Вы не смогли угадать число", n)
